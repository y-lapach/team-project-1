$(document).ready(function () {
    $('.slider-wrapper').slick({
        arrows: false,
        dots: true,
        fade:true,
        cssEase: 'linear',
        speed: 400,
        easing:'ease',
    });
});